source 'https://rubygems.org'

# New rubies have problems with SSHkit' formatter
# Also node@9 fails to build assets on MacOS
ruby File.read(File.join(__dir__, '.ruby-version'))

gem 'rake'
gem 'sshkit', '~> 1.7.1'
gem 'foreman'
